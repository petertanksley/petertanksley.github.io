# motif_refs/ — reference images for the article-sticker vocabulary

Committed, unlike `../refs/` (personal photos, gitignored). Each file is cited from
`../motifs.yml` as `[motif_refs/<name>]` and bananarama attaches it to the prompt.

| File | What | Provenance |
|------|------|------------|
| `coin.png` | Tri-service bronze challenge coin: fire-service Maltese cross, blank shield, rod and serpent. The responders object. | Peter, 2026-09-18: pipeline candidate `coin_test-1` edited in Gemini for a recognisable cross, then cropped out of its hex frame here. |
